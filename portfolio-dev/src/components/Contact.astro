---
import { Icon } from "astro-icon/components";

interface SocialLink {
  name: string;
  url: string;
  icon: string;
  showInContact: boolean;
}

interface LocaleData {
  home: {
    name: string;
    socials: SocialLink[];
  };
  contact: {
    title: string;
    subtitle: string;
  };
  footer: {
    copyright: string;
    builtWith: string;
    sourceCode: string;
    repo: string;
  };
}

const { locale } = Astro.props as { locale: LocaleData };
---

<section id="contact" class="section-container">
  <div class="container mx-auto">
    <div class="space-y-12">
      <div class="contact-header opacity-0 mb-16">
        <h2
          class="text-3xl md:text-4xl font-heading font-bold text-maintext mb-2 flex items-center gap-4"
        >
          <span class="text-accent">/</span>
          {locale.contact.title}
        </h2>
        <p class="text-subtext font-body text-sm max-w-md">
          {locale.contact.subtitle}
        </p>
      </div>

      <div
        class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 text-center"
      >
        {
          locale.home.socials
            .filter(
              (social) =>
                social.showInContact &&
                social.url &&
                social.url !== "#" &&
                social.url !== "",
            )
            .map((social) => (
              <a
                href={social.url}
                target={social.name === "Email" ? "_self" : "_blank"}
                class="contact-card opacity-0 group p-8 bg-accent/5 border border-accent/10 rounded-2xl hover:border-accent/40 hover:bg-accent/10 transition-all duration-300"
              >
                <Icon
                  name={social.icon}
                  class="w-10 h-10 text-accent mb-4 mx-auto group-hover:scale-110 transition-transform"
                />
                <h3 class="text-maintext font-bold mb-1">{social.name}</h3>
                <p class="text-subtext text-xs truncate px-2">
                  {social.url.replace("https://", "").replace("mailto:", "")}
                </p>
              </a>
            ))
        }
      </div>

      <!-- /* FOOTER */ -->
      <div class="contact-footer opacity-0 pt-5 border-t border-white/5">
        <div
          class="flex flex-col md:flex-row justify-between items-center gap-4"
        >
          <p class="text-subtext text-sm font-body order-1 md:order-1">
            {locale.footer.copyright}
          </p>
          <p class="text-subtext text-sm font-body order-2 md:order-2">
            {locale.footer.builtWith}
          </p>
          <div class="flex items-center gap-1 order-3">
            <span class="text-subtext text-sm font-body"
              >{locale.footer.sourceCode}</span
            >
            <a
              href={locale.footer.repo}
              target="_blank"
              class="flex items-center gap-1 text-subtext text-sm font-body hover:text-accent transition-colors"
            > GitHub
              <Icon name="mdi:github" class="w-4 h-4" />
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Header & Footer Initial State */
  .contact-header,
  .contact-footer {
    transform: translateY(20px);
    transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }

  /* Cards Initial State */
  .contact-card {
    transform: translateY(20px) scale(0.95);
    transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }

  /* Shared Revealed State */
  .contact-header.reveal,
  .contact-card.reveal,
  .contact-footer.reveal {
    opacity: 1 !important;
    transform: translateY(0) scale(1);
  }
</style>

<script>
  const revealContact = () => {
    const header = document.querySelector(".contact-header");
    const footer = document.querySelector(".contact-footer");
    const cards = document.querySelectorAll(".contact-card");

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const target = entry.target as HTMLElement;

            if (target.classList.contains("contact-card")) {
              const index = Array.from(cards).indexOf(target);
              // Stagger effect for the 4-column grid
              setTimeout(
                () => {
                  target.classList.add("reveal");
                },
                (index % 4) * 150,
              );
            } else {
              // Header and Footer reveal immediately
              target.classList.add("reveal");
            }

            observer.unobserve(target);
          }
        });
      },
      {
        threshold: 0.15,
        rootMargin: "0px 0px -10px 0px",
      },
    );

    if (header) observer.observe(header);
    if (footer) observer.observe(footer);
    cards.forEach((card) => observer.observe(card));
  };

  // Runs on initial load
  window.addEventListener("load", revealContact);

  // Support for Astro View Transitions
  document.addEventListener("astro:after-swap", revealContact);
</script>
